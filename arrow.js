/*  ========= Arrow function ===============  **/

//multiline
const doMath = (x , y) => {
    const sum = x + y;
    const diff = x - y;
    const total = sum * diff;
    return total
}
// single line
const doubleIt = num => num * 2;

const sum = (x , y) => x + y;